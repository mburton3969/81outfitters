<h1 class="logo pull-left">
	{% if (logo) %} 
	<a href="{{ home }}">
		<img src="{{ logo }}" title="{{ name }}" alt="{{ name }}" class="img-responsive" style="margin-top:-15px;">
	</a>
	{% else %} 
	<a href="{{ home }}"> <span>{{ name }} </span>	</a>
	{% endif %} 
</h1>